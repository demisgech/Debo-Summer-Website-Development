function square(number) {
  return number * number;
}

const square1 = function (number) {
  return number * number;
};
// Arrow function (labmda expression)

const square2 = (number) => number * number;

console.log(square(4));
